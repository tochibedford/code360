import Navbar from './Navbar'
import Hero from './Hero'

const Header = ()=>{
    return(
        <header id="navbar">
            <Navbar />
            <Hero />
        </header>
    )
}

export default Header;